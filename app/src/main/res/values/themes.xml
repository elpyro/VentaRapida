<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="Theme.VentaRapida" parent="Theme.MaterialComponents.DayNight.DarkActionBar">
        <!-- Primary brand color. -->
        <item name="colorPrimary">@color/purple_500</item>
        <item name="colorPrimaryVariant">@color/purple_700</item>
        <item name="colorOnPrimary">@color/white</item>
        <!-- Secondary brand color. -->
        <item name="colorSecondary">@color/teal_200</item>
        <item name="colorSecondaryVariant">@color/teal_700</item>
        <item name="colorOnSecondary">@color/black</item>
        <!-- Status bar color. -->
        <item name="android:statusBarColor" tools:targetApi="l">?attr/colorPrimaryVariant</item>
        <!-- Customize your theme here. -->
    </style>

    <style name="Theme.VentaRapida.NoActionBar">
        <item name="windowActionBar">false</item>
        <item name="windowNoTitle">true</item>
    </style>

    <style name="AppTheme" parent="Theme.AppCompat.Light.NoActionBar">
        <item name="android:statusBarColor">#FF0000</item>
    </style>

    <style name="Theme.VentaRapida.AppBarOverlay" parent="ThemeOverlay.AppCompat.Dark.ActionBar" />

    <style name="Theme.VentaRapida.PopupOverlay" parent="ThemeOverlay.AppCompat.Light" />

    <style name="SnackbarError" parent="Widget.Design.Snackbar">
        <item name="android:background">@color/rojo</item>
        <item name="android:textColor">@color/white</item>
    </style>

    <style name="ApareciaTextoMenu">
        <item name="android:textSize">18sp</item>
        <item name="color">@color/white</item>
    </style>

    <style name="ColorFuentes">
        <item name="android:textColor">@color/black</item>
    </style>

    <style name="ColorFuenteEnFondoGris">
        <item name="android:textColor">@color/white</item>
    </style>

    <style name="MenuTitle" parent="@style/TextAppearance.AppCompat.Widget.ActionBar.Title">
        <item name="android:textSize">20sp</item>
    </style>

    <!-- Base application theme. -->
    <style name="Base.Theme.VentaRapida" parent="Theme.Material3.DayNight.NoActionBar">
        <!-- Customize your light theme here. -->
        <!-- <item name="colorPrimary">@color/my_light_primary</item> -->
    </style>


</resources>